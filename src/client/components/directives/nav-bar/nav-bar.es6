'use strict';

app.directive('navBar', ($timeout, API, $state) => {
    return {
        templateUrl: 'nav-bar.html',
        scope: {},

        link(scope, element, attrs) {

            var init = () => {
            };

            init();
        }
    }
});
