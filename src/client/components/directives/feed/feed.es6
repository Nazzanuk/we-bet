'use strict';

app.directive('feed', ($timeout, API, $state) => {
    return {
        templateUrl: 'feed.html',
        scope: {},

        link(scope, element, attrs) {

            var init = () => {
            };

            init();
        }
    }
});
